torch>=2.10.0
torchvision>=0.25.0
ultralytics>=8.4.24
pyyaml>=6.0
tqdm>=4.67
ftfy>=6.3
regex>=2024.0
lap>=0.5
clip @ https://github.com/openai/CLIP/archive/refs/heads/main.zip

# --- plate.py (multi-frame license plate reading) ---
# fast-alpr bundles open-image-models (YOLOv9-t plate detector) + fast-plate-ocr
# (plate-specialized OCR with per-character confidences). Chosen over Tesseract:
# purpose-built for plates, runs on CPU via ONNX, confidences needed for voting.
fast-alpr>=0.4.0
# WPODNet corner regression: rectifies tilted plates to a frontal view before
# OCR (measured +14-17pp on oblique plates, ECCV18 ablation). Weights
# (wpodnet.pth) auto-download to the app models dir on first use.
wpodnet-pytorch>=1.1.1
# onnxruntime is an OPTIONAL extra of fast-plate-ocr 1.x — must be listed
# explicitly here or the OCR/detector models cannot run at all.
onnxruntime>=1.27.0
# CSRT/KCF trackers used by plate.py exist ONLY in the contrib build.
# WARNING: ultralytics pulls plain opencv-python and fast-plate-ocr pulls
# opencv-python-headless; all three ship the SAME `cv2` directory and the one
# pip installs last silently wins. After installing this file, force the
# contrib build on top (see README):
#   pip install --force-reinstall --no-deps "opencv-contrib-python>=5.0"
opencv-contrib-python>=5.0
